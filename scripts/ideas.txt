- Boutons :
    classe bouton 
        is_on : False
        if is_on : give a direction (or be wild)
    reset after each garbage spawn

- Garbage : 
    if garbage scale > max scale : 
        ship hit (ship pv -= 1 + load cracklings on cockpit)
        destroy garbage 
    else if reticles cross position at garbage :
        if red button pressed :
            garbage destroy
        garbage counter += 1
